<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>KeySlim Drops — Natural Weight Support | Smart Store Global</title>
  <meta name="description" content="Discover KeySlim Drops — a natural, plant-based weight support formula that boosts metabolism, reduces cravings, and promotes energy. Learn more and order now.">
  <meta name="author" content="Smart Store Global" />
  <link rel="icon" href="/logo.jpg" type="image/jpeg" />
  <link rel="apple-touch-icon" href="/logo.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    :root {
      --bg: #0f1115;
      --card: #151821;
      --text: #f9f9f9;
      --muted: #c9c9c9;
      --accent: #ffd000;
      --accent2: #ffe24d;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
    }
    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 28px;
    }
    /* Header */
    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 14px 16px;
      background: #111;
      color: #fff;
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }
    .brand { display: flex; align-items: center; gap: 12px; }
    .brand .logo img { height: 42px; width: auto; border-radius: 8px; display: block; }
    .brand-name { font-family: "Nunito Sans", sans-serif; font-weight: 800; font-size: 18px; }
    .tagline { font-size: 12px; color: #cfcfcf; margin-top: 2px; }
    .nav-actions { display: flex; align-items: center; gap: 12px; }
    .dropdown { position: relative; }
    .dropbtn {
      background: var(--accent2);
      color: #111;
      border: none;
      border-radius: 10px;
      padding: 10px 14px;
      font-weight: 800;
      cursor: pointer;
    }
    .dropdown-menu {
      position: absolute;
      right: 0;
      top: 110%;
      background: #fff;
      color: #222;
      min-width: 220px;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.12);
      padding: 6px;
      display: none;
      z-index: 200;
    }
    .dropdown-menu a {
      display: block;
      padding: 10px 12px;
      border-radius: 8px;
      color: #222;
      text-decoration: none;
    }
    .dropdown-menu a:hover { background: #f3f4f6; }
    .dropdown.open .dropdown-menu { display: block; }

    /* Page Content */
    .hero {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
      margin-top: 40px;
    }
    .hero img {
      width: 100%;
      max-width: 420px;
      border-radius: 12px;
      background: #0b0b0b;
      object-fit: contain;
    }
    .hero-text { flex: 1; min-width: 280px; }
    .hero-text h1 {
      font-size: clamp(26px, 3.5vw, 38px);
      font-weight: 800;
      color: #fff;
      margin: 0 0 12px;
    }
    .hero-text p { color: var(--muted); line-height: 1.7; font-size: 16px; max-width: 600px; }

    .cta-btn {
      background: var(--accent);
      color: #111;
      padding: 14px 26px;
      border-radius: 999px;
      font-weight: 800;
      text-decoration: none;
      display: inline-block;
      margin-top: 20px;
    }
    .cta-btn:hover { background: var(--accent2); }

    /* Benefits Section */
    .benefits { margin-top: 50px; }
    .benefits h2 { font-size: 24px; font-weight: 800; margin-bottom: 20px; }
    .benefit-list { display: grid; gap: 14px; }
    .benefit-item { background: var(--card); padding: 16px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); }
    .benefit-item strong { color: var(--accent2); }

    /* Footer */
    .socials { margin-top: 40px; text-align: center; }
    .socials a { margin: 0 6px; text-decoration: none; }
    .socials a.facebook { color: #3b5998; }
    .socials a.instagram { color: #E4405F; }
    .footer-links { text-align: center; margin-top: 14px; }
    .footer-links a {
      color: var(--muted);
      margin: 0 8px;
      text-decoration: none;
      font-size: 14px;
    }
    .footer-links a:hover { text-decoration: underline; }
    .footer {
      margin-top: 12px;
      color: #a7a7a7;
      font-size: 12px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">

    <!-- HEADER -->
    <header class="topbar">
      <div class="brand">
        <a href="/" class="logo"><img src="logo.jpg" alt="Smart Store Global" /></a>
        <div class="brand-text">
          <strong class="brand-name">Smart Store Global</strong>
          <div class="tagline">Deals, reviews & smarter shopping</div>
        </div>
      </div>
      <div class="nav-actions">
        <div class="dropdown" id="categoryMenu">
          <button class="dropbtn" id="dropToggle">Explore Categories ▾</button>
          <div class="dropdown-menu">
            <a href="health.html">Health</a>
            <a href="#cat-fitness">Fitness</a>
            <a href="#cat-tech">Tech</a>
            <a href="#cat-beauty">Beauty</a>
            <a href="#cat-baby">Baby</a>
            <a href="#cat-parenting">Parenting</a>
          </div>
        </div>
      </div>
    </header>

    <!-- HERO SECTION -->
    <section class="hero">
      <div class="hero-text">
        <h1>KeySlim Drops — Plant-Based Weight Support</h1>
        <p>
          KeySlim Drops are an advanced natural formula that helps support weight loss by boosting metabolism,
          enhancing energy, and curbing cravings. Powered by herbal extracts and vitamins, these drops fit easily into
          your daily wellness routine.
        </p>
        <a class="cta-btn" href="https://getkeyslimdrops.cc/vsl1/#aff=talhaashraf1108" target="_blank" rel="nofollow noopener">
          Visit Official Page →
        </a>
      </div>
      <img src="keyslim-bottle.jpg" alt="KeySlim Drops Bottle">
    </section>

    <!-- BENEFITS -->
    <section class="benefits">
      <h2>Why Choose KeySlim Drops?</h2>
      <div class="benefit-list">
        <div class="benefit-item"><strong>🌿 Natural Formula:</strong> Made from herbal extracts, vitamins, and antioxidants that promote healthy weight management.</div>
        <div class="benefit-item"><strong>⚡ Boosts Metabolism:</strong> Helps the body burn fat efficiently while maintaining steady energy throughout the day.</div>
        <div class="benefit-item"><strong>💧 Easy to Use:</strong> Liquid form ensures faster absorption compared to capsules or pills.</div>
        <div class="benefit-item"><strong>🍋 Appetite Control:</strong> Reduces unhealthy cravings and supports mindful eating habits.</div>
      </div>
      <a class="cta-btn" href="https://getkeyslimdrops.cc/vsl1/#aff=talhaashraf1108" target="_blank" rel="nofollow noopener">
        Order KeySlim Drops Now →
      </a>
    </section>

    <!-- FOOTER -->
    <div class="socials">
      <a href="https://www.facebook.com/profile.php?id=61582590449680" class="facebook" target="_blank"><i class="fab fa-facebook-f fa-lg"></i></a>
      <a href="https://www.instagram.com/the.smartshop1/" class="instagram" target="_blank"><i class="fab fa-instagram fa-lg"></i></a>
    </div>
    <div class="footer-links">
      <a href="index.html">Home</a>
      <a href="about.html">About Us</a>
      <a href="privacy-policy.html">Privacy Policy</a>
      <a href="terms-conditions.html">Terms & Conditions</a>
      <a href="contact.html">Contact Us</a>
    </div>
    <p class="footer">© THE SMART SHOP • Smart Picks. Smarter Living.</p>
  </div>

  <!-- Dropdown Script -->
  <script>
    (function(){
      const menu = document.getElementById("categoryMenu");
      const toggle = document.getElementById("dropToggle");
      if(!menu || !toggle) return;
      toggle.addEventListener("click", e => {
        e.stopPropagation();
        menu.classList.toggle("open");
      });
      document.addEventListener("click", e => {
        if(!menu.contains(e.target)) menu.classList.remove("open");
      });
      document.addEventListener("keydown", e => {
        if(e.key === "Escape") menu.classList.remove("open");
      });
    })();
  </script>
</body>
</html>
